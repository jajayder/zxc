---
- name: Descobrir Secrets de cada cluster autenticado
  ansible.builtin.include_tasks: discover_cluster.yml
  loop: "{{ ocp_sessions }}"
  loop_control:
    loop_var: ocp_session
    label: "{{ ocp_session.cluster.name }} ({{ ocp_session.cluster.environment }})"
